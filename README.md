If you custom icons does not work, ensure you have vsicons extnesion isntalled and run command in vscode "Icons: Apply Icons Customization"

# ADRs:

## Attribute names - kebab case

Kebab case is ONLY used by BaseElement, developer should never use kebab-case directly, all camelCase(templates, states, onChange callbacks)
