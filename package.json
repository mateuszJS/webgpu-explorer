{
  "name": "webgpu-explorer",
  "version": "0.0.1",
  "description": "Website to explore WebGPU API.",
  "main": "src/index.ts",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "start": "export $(cat .env | xargs) && export NODE_ENV=development && webpack serve --open --mode=development",
    "build": "export $(cat .env | xargs) && export NODE_ENV=production && webpack --mode=production "
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "css-loader": "^7.1.1",
    "html-webpack-plugin": "^5.5.0",
    "mini-css-extract-plugin": "^2.9.0",
    "sass": "^1.75.0",
    "sass-loader": "^14.2.1",
    "style-loader": "^4.0.0",
    "ts-loader": "^9.4.2",
    "typescript": "^5.2.2",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^4.11.1"
  }
}
